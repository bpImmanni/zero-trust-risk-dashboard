import { Component } from '@angular/core';

@Component({
  selector: 'event-log',
  standalone: true,
  template: `<p style="opacity:.8">Event log UI placeholder (wired to Redis later).</p>`
})
export class EventLogComponent {}
